{{- if .Values.httpServer.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "observatory-server.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "observatory-server.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels: {{- include "observatory-server.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "observatory-server.selectorLabels" . | nindent 8 }}
      annotations: {{- toYaml .Values.podAnnotations | nindent 8 }}
    spec:
      serviceAccountName: {{ include "observatory-server.serviceAccountName" . }}
      containers:
        - name: observatory-server
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - serve-http
            - --port={{ .Values.httpServer.port }}
            - --prom-url={{ .Values.prometheus.url }}
            {{- if .Values.llm.offline }}
            - --no-llm
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.httpServer.port }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources: {{- toYaml .Values.resources | nindent 12 }}
{{- end -}}
